{
  "name": "hvsdcm-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "db:init": "wrangler d1 migrations apply hvsdcm --local",
    "test": "node --test abc-paper-experiment.test.mjs ../scripts/behavior-lab.test.mjs live-trading.test.mjs paper-report.test.mjs test.mjs password-reset.test.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
